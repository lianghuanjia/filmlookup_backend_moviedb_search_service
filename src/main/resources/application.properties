spring.application.name=search_service

# Disable Spring Boot Banner
spring.main.banner-mode=off

# Load the database config and sensitive data in the application-secrets.properties file
spring.config.import=classpath:application-secrets.properties

# Port and host setting
server.port=8080
server.address=0.0.0.0

# Logging setting
logging.level.com.example.movie_service=INFO
logging.file.name=
logging.file.path=