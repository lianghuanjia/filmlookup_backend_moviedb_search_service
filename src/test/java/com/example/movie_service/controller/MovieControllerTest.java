package com.example.movie_service.controller;

import com.example.movie_service.service.MovieService;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

/**
 * @ExtendWith(MockitoExtension.class) is for JUnit 5
 * The extension is also used to initialize the mocks.
 */
@ExtendWith(MockitoExtension.class)
public class MovieControllerTest {

//    /**
//     * @Mock is used to create and inject mock instances of dependencies. This allows you to control the behavior of
//     * these dependencies
//     */
//    @Mock
//    private MovieService movieService;
//
//    /**
//     * The @InjectMocks annotation is used to create an instance of the class under test and inject the mocks
//     * (annotated with @Mock or @Spy) into it
//     */
//    @InjectMocks
//    private MovieController movieController;
//
//    @Test
//    public void testSearchMovies() {
//
//    }
}
